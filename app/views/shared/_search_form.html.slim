= form_tag search_path, method: :get do
  = text_field_tag 'search_what', params[:search_what], type: 'search', placeholder: 'Search'
  = select_tag 'search_resource', options_for_select(Search::SEARCH_RESOURCES, params[:search_resource])
  = submit_tag 'Search'


